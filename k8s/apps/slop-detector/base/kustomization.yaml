apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - deployment.yaml
  - service.yaml
  - istio-gateway.yaml
  - istio-virtualservice.yaml

commonLabels:
  app: slop-detector

# Optional: Add configMapGenerator for environment-specific config
# configMapGenerator:
#   - name: slop-detector-config
#     literals:
#       - NODE_ENV=production

# Optional: Add secretGenerator for sensitive data
# secretGenerator:
#   - name: slop-detector-secrets
#     literals:
#       - openai-api-key=placeholder
#       - anthropic-api-key=placeholder
